import TreatmentPage from "../companent/TreatmentPage"
import { wound_stitching } from '../data/wound_stitching';

const Wound_stitching = () => {
return <TreatmentPage pageData={wound_stitching} />;
};

export default Wound_stitching